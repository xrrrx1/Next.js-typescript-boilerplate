import * as React from "react";
import Title from "../components/Title";

export const App: React.FunctionComponent = () => (
  <div>
    <Title titleColor="red" />

    <span>default</span>
  </div>
);
